import styled from "styled-components";

const Modal = () => {
  const style = {
    width: "100px",
    height: "100px",
    display: "flex",
  };

  const deleteModal = () => {};
  return (
    <>
      <div style={style}>
        <button>모달 삭제</button>
      </div>
    </>
  );
};
export default Modal;
